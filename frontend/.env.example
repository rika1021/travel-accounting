# Frontend: set backend target for the server-side proxy.
# Recommended for local dev:
PUBLIC_API_BASE_URL=http://localhost:3000

# If you set PUBLIC_API_BASE_URL=mock the frontend will use the built-in mock API.
